# .env.example
PORT=3000
CACHE_TTL=15      # cache TTL in seconds for quote results
CACHE_MAX_SIZE=500 # max number of distinct cache keys (LRU capacity)
# If you switch to an API that requires a key, add here:
# UPSTREAM_API_KEY=your_key_here
